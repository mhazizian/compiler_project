class Main
{
        def main(): int
        {
                var arr : int[];
                arr = new int[13];
                writeln(arr.length);
                return 0;
        }
}

class ClassTest {
        var arr : int[];

        def methodTest(): int {
                arr = new int[13];
                writeln(arr.length);
                return 0;
        } 
}