class Main
{
        def main(): int
        {
                var flag : boolean;
                var arr : int[];
                arr = new int[13];

                if(flag) then
                        writeln(arr.length);

                if(true) then
                        writeln("Salam");                        

                return 0;
        }
}