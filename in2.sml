class Main
{
        def main(): int
        {
                writeln(2);
                writeln("salam");
                return 0;
        }
}

class ClassTest {
        var arr : int[];

        def methodTest(): int {
                arr = new int[13];
                writeln(arr.length);
                return 0;
        } 
}